import { Container, TextInput } from './styles';

export const SearchArea = () => {
    return (
        <Container>
            <TextInput
            placeholder='Digite o personagem'  />
        </Container>
    );
}